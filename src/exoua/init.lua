return {
    binary = require("exoua.binary"),
    reader = require("exoua.reader"),
    writer = require("exoua.writer"),
    sections = require("exoua.sections"),
    types = require("exoua.types")
}
